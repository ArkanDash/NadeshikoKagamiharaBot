import { MessageEmbed } from 'discord.js'

const command = {
    name:"Insert Name Command",
    description:"Description of the command",
    async execute(msg, client){
        
    }
}

export default command;